<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="informatico.to.log_inapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="IU DIGITAL"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Log_InApp">
        <activity android:name=".Curso4"></activity>
        <activity android:name=".Curso3" />
        <activity android:name=".Curso2" />
        <activity android:name=".Curso1" />
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegistroData.RegistroData" />
        <activity android:name=".Principal.PrincipalMenu" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>